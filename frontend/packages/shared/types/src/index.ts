/**
 * @compliance-desk/shared/types
 *
 * TypeScript types generated from FastAPI OpenAPI schema (per AD-9).
 *
 * Phase 0: scaffold only.
 * Phase 1: codegen integrates here alongside api-client.
 */

export const _placeholder = 'types scaffold (Phase 1 will populate via codegen)'
