/**
 * @compliance-desk/shared/ui
 *
 * Design tokens + reusable UI primitives (per AD-9).
 *
 * Phase 0: design tokens only (Tailwind preset).
 * Components will be extracted from compliance-frontend in subsequent phases
 * as backoffice-frontend and client-portal-frontend grow.
 *
 * See ./tailwind-preset.js for shared Tailwind configuration.
 */

export { tailwindPreset } from './tailwind-preset'
