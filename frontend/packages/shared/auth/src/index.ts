/**
 * @compliance-desk/shared/auth
 *
 * Shared auth utilities: JWT handling, refresh logic (per AD-9).
 *
 * Phase 0: scaffold only. Existing auth code stays in
 * compliance-frontend/src/store/authStore.ts until Phase 1 extraction.
 *
 * Phase 1: extract authStore + JWT refresh interceptor here.
 */

export const _placeholder = 'auth scaffold (Phase 1 will extract authStore here)'
